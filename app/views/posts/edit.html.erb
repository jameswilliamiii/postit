<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing post</h1>

  <%= render "form", post: @post, submit_text: "Update post" %>

  <%= render RBUI::Link.new(variant: :secondary, href: posts_path, class: "ms-3") { "Cancel" } %>
</div>
